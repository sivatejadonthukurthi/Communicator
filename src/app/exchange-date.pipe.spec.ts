import { ExchangeDatePipe } from './exchange-date.pipe';

describe('ExchangeDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ExchangeDatePipe();
    expect(pipe).toBeTruthy();
  });
});
